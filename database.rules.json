{
    "rules": {
        ".read": true,
        "users": {
            "$uid": {
                ".read": "$uid === auth.id",
                ".write": "$uid === auth.uid"
            }
        }
    }
}

<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{ title }}</title>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <link href="/css/style.css" rel="stylesheet">
        <script src="/js/script.js"></script>
        <link href="/css/prism.css" rel="stylesheet">
        <script src="/js/prism.js"></script>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
        <script src="https://cdn.quilljs.com/1.3.6/quill.min.js"></script>
        <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
        <link href="https://cdn.quilljs.com/1.3.6/quill.bubble.css" rel="stylesheet">
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                <a class="navbar-brand" href="/">{{ title }}</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        {% if config.navbar.home %}
                        <li class="nav-item">
                            <a class="nav-link" href="/">Home</a>
                        </li>
                        {% endif %}
                        {% if config.navbar.pages %}
                        <li class="nav-item btn-group">
                            <a class="nav-link" href="/pages">Pages</a>
                            <a class="nav-link dropdown-toggle dropdown-toggle-split" data-bs-toggle="pagesDropdown" aria-expanded="false">
                                <span class="visually-hidden"></span>
                            </a>
                            <ul class="dropdown-menu">
                                {% for page in custompages %}
                                <li><a class="dropdown-item" href="/pages/{{ page }}">{{ page | capitalize }}</a></li>
                                {% endfor %}
                            </ul>
                        </li>
                        {% endif %}
                        {% if config.navbar.forums %}
                        <li class="nav-item">
                            <a class="nav-link" href="/forums">Forums</a>
                        </li>
                        {% endif %}
                    </ul>
                    <ul class="navbar-nav navbar-right">
                        {% if user.email %}
                            <li class="nav-item">
                                <li><a class="nav-link" href="/account"><span><span class="material-icons">account_circle</span> Profile</span></a></li>
                                <li><a class="nav-link" href="/logout"><span><span class="material-icons">logout</span> Logout</span></a></li>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <li><a class="nav-link" href="/register"><span><span class="material-icons">meeting_room</span> Register</span></a></li>
                            </li>
                            <li class="nav-item">
                                <li><a class="nav-link" href="/login"><span><span class="material-icons">login</span> Login</span></a></li>
                            </li>
                        {% endif %}
                    </ul>
                    <form class="d-flex" action="/search">
                        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                        <button class="btn btn-outline-success" type="submit">Search</button>
                    </form>
                </div>
            </div>
        </nav>
        <br />
        <div class="container">
            {% for alert in alerts %}
                <div class="alert alert-{{ alert.type }} alert-dismissible fade show" role="alert">
                    {{ alert.text }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% endfor %}
        </div>
        <div class="container">
            {% block content %}{% endblock %}
        </div>
        <div class="footer">
            &copy; <span class="year"></span> <a href="{{ config.author.url }}">{{ config.author.name }}</a>
        </div>
    </body>
</html>